# Next.js Environment Variables
# ==============================
# Next.js automatically loads environment files in this order:
#   .env.development.local - Local dev overrides (gitignored)
#   .env.development       - Development defaults (can commit)
#   .env.local             - Local overrides for any env (gitignored)
#   .env                   - Defaults (can commit)
#
# For Vercel deployments, pull env vars with:
#   pnpm env:pull:vercel:development  → .env.development.local
#   pnpm env:pull:vercel:production   → .env.production.local
#
# Create your local env:
#   cp .env.example .env.development.local

# =============================================================================
# CONVEX (required)
# =============================================================================
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# =============================================================================
# APP CONFIG
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
