# Environment Variables Reference (Monorepo Root)
# ================================================
# This file documents environment variables. DO NOT copy this file.
# Each app/package has its own env file pattern:
#
# NEXT.JS (apps/web):
#   .env.development       - Development defaults
#   .env.development.local - Local overrides (gitignored)
#   .env.production.local  - Production testing (gitignored)
#   Pull from Vercel: pnpm env:pull:vercel
#   See: apps/web/.env.example
#
# EXPO (apps/native):
#   .env.development - Development (local + EAS dev builds)
#   .env.production  - Production (EAS prod builds)
#   Note: EAS Build needs env vars in eas.json or EAS Secrets
#   See: apps/native/.env.example
#
# CONVEX (packages/convex):
#   .env.convex.development - Convex dev env vars (local reference)
#   .env.convex.production  - Convex prod env vars (local reference)
#   Pull: pnpm env:pull:convex
#   Push: pnpm env:push:convex:development
#   See: .env.convex.example
#
# CONVEX CLI DEPLOY KEYS (root level):
#   .env.convex-cli.development - Dev deploy key (optional)
#   .env.convex-cli.production  - Prod deploy key
#   .env.convex-cli.preview     - Preview deploy key
#   See: .env.convex-cli.example

# =============================================================================
# NEXT.JS VARIABLES (apps/web)
# =============================================================================
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# EXPO VARIABLES (apps/native) - see apps/native/.env.example
# =============================================================================
# EXPO_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
